# Pinned: the 5.0.0 headless wheel available at the time of writing shipped
# without cv2.CascadeClassifier (objdetect bindings missing) — 4.10 is the
# version these notebooks were actually verified against.
opencv-python-headless==4.10.0.84
numpy>=1.26
matplotlib>=3.8
pillow>=10.0
scikit-image>=0.22
imageio>=2.34
imageio-ffmpeg>=0.4
scikit-learn>=1.4
qrcode>=8.0
python-barcode>=0.16
notebook>=7.0
nbformat>=5.9
nbclient>=0.9
ipython>=8.20
